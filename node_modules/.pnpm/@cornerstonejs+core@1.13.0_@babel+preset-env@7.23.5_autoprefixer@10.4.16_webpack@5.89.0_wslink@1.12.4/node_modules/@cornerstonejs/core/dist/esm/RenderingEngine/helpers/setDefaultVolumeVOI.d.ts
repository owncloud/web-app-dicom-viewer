import { VolumeActor, IImageVolume } from '../../types';
declare function setDefaultVolumeVOI(volumeActor: VolumeActor, imageVolume: IImageVolume, use16BitTexture: boolean): Promise<void>;
export default setDefaultVolumeVOI;
